---
resources:
- name: input
  type: git
  source:
#    uri: https://github.com/SergeyMuha/concoure-dora-push.git
    uri: https://github.com/ihuston/python-cf-examples.git

- name: resource-deploy-web-app
  type: cf
  source:
    api: https://api.gcp.sergeymuha.altoros.com
    username: admin
    password: eoqn5vln91htjki9wk4q
    organization: altoros
    space: altoros
    skip_cert_check: true

jobs:
- name: job-first
  public: true
  plan:
  - get: input
  - task: first
    config:
      platform: linux
      image_resource:
        type: docker-image
        source: {repository: ubuntu}
      inputs:
      - name: input
      run:
        dir: input
        path: ./untar.sh
      outputs:
      - name: output
#  - task: second
#    config:
#      platform: linux
#      image_resource:
#        type: docker-image
#        source: {repository: ubuntu}
#      inputs:
#      - name: output
#      run:
#        dir: files
#        path: ls
#        args: [-laR]

  - put: resource-deploy-web-app
    params:
#      path: 
      #manifest: output/staging_info.yml
      manifest: output/01-simple-python-app/manifest.yml
#      current_app_name: dora
